apiVersion: v1
kind: Service
metadata:
  name: fontend-service
  namespace: memories-notes-app-namespace
  labels:
    app: frontend
spec:
  type: NodePort
  selector:
    app: frontend
  ports:
    - name: http
      protocol: TCP
      port: 3000
      targetPort: 3000
      nodePort: 30001