kind: Service
apiVersion: v1
metadata:
  name: mongodb-service
  namespace: memories-notes-app-namespace
  labels:
    app: mongodb

spec:
  clusterIP: None
  selector:
    app:  mongodb

  ports:
    - port: 27017
      targetPort: 27017