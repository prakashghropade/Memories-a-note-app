kind: Service
apiVersion: v1
metadata:
  name: mongodb-service
  namespace: memories-notes-app-namespace

spec:
  clusterIP: None
  selector:
    app:  mongodb

  ports:
    - port: 27017
      targetPort: 27017