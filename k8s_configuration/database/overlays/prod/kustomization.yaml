apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: memories-notes-prod

resources:
  - ../../base

patches:
  - path: pv-patch.yaml

images:
  - name: mongo
    newName: mongo
    newTag: 8.0.4