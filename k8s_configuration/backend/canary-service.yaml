apiVersion: v1
kind: Service
metadata:
  name: backend-canary
  namespace: memories-notes-app-namespace
  labels:
    app: backend
spec:
  type: NodePort
  selector:
    app: backend
  ports:
    - name: http
      protocol: TCP
      port: 3001
      targetPort: 3001
      nodePort: 30002