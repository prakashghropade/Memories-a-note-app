apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: memories-notes-app-namespace
spec:
  type: NodePort
  selector:
    app: backend
  ports:
    - name: http
      protocol: TCP
      port: 3001
      targetPort: 3001
      nodePort: 30002